//
//  ContentView.swift
//  Anything
//
//  Created by Scholar on 6/5/24.
//

import SwiftUI

struct ContentView: View {
    var body: some View {
        VStack {
            Image(systemName: "globe")
                .imageScale(.large)
                .foregroundStyle(.tint)
            Text("Hello, Ananya and Audrey!")
                .foregroundColor(Color.orange)
        }
        .padding()
    }
}

#Preview {
    ContentView()
}
